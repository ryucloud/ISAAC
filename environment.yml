name: myenv
channels:
  - defaults
dependencies:
  - python=3.9
  - cpuonly
  - pytorch
  - transformers
  - zstandard
  - pip:
    - fasttext
    - pyahocorasick
  
prefix: /opt/anaconda3/envs/myenv
